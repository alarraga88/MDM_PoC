import React from 'react';
import AppRoutes from './routes/AppRoutes';

/**
 * Root application component.
 * Routes and auth are handled within AppRoutes.
 */
const App = () => {
  return <AppRoutes />;
};

export default App;
